package com.gildedrose;

public class MyStepdefs {
    public MyStepdefs() {
        Given("^Item \"([^\"]*)\" with SellIn (\\d+) and Quality (\\d+)$", (String arg0, Integer arg1, Integer arg2) -> {

        });
    }
}
